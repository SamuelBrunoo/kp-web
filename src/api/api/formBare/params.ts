export type TApi_Params_FormBare = {
  formBare: {
    model: { modelId?: string }
    order: { orderId?: string }
    client: { clientId?: string }
    representative: { representativeId?: string }
  }
}
